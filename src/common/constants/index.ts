export enum MODELS {
  USERS = 'USERS',
  TOKENS = 'TOKENS',
}

export enum TokenTypes {
  ACCESS = 'access',
  REFRESH = 'refresh',
  RESET_PASSWORD = 'resetPassword',
}

export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
}
